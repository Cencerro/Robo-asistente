services:
  automata:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file: ../.env
    volumes:
      - automata_data:/app/data
      - automata_logs:/app/logs

volumes:
  automata_data:
  automata_logs:
